#include <math.h>
#include <stdlib.h>
#include <stdio.h>

void solve(int c, int *pos, int n)
{
    print
    for i 
        printf("%d%s", pos[i], i < n- 1 ?)

    return;

    for (r <n)
    {
        ok = 1;
        for ( i < c)
            ok =0
        if (ok)
            pos[i] = abs(pos[i] - r) == abs(i - c);
        
        pos[r] = c;
        solve
    }
}